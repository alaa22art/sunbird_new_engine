define(['app'], function (app) {
    'use strict';
    app.controller('homeCtrl', [
        '$scope',
        '$state',
        'homeService',
        function (
            $scope,
            $state,
            homeService
        ) {
        }
    ]);
});
